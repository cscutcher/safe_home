#!/bin/bash
set -o nounset
set -o errexit
set -o pipefail

DEVICE="$1"
HOSTNAME="$(hostname)"

IP_ADDRESS=$(nmcli -f IP4.ADDRESS d show $DEVICE | egrep -o "[0-9.]+/24$" | egrep -o "^[0-9.]+")
tmate show-messages | sed -e "s/$HOSTNAME/$IP_ADDRESS/g"
